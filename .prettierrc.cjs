/** @type {import("prettier").Config} */
module.exports = {
	/* ------------------------------------------------------------ doc.printer.Options ------------------------------------------------------------- */
	printWidth: 200,

	useTabs: true,

	/* -------------------------------------------------------------- RequiredOptions --------------------------------------------------------------- */
	singleQuote: true,

	trailingComma: 'all',

	arrowParens: 'avoid',

	singleAttributePerLine: true,
};
